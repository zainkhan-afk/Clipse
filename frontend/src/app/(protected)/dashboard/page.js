"use client";
import { useRouter } from "next/navigation";

export default function Login() {
    const router = useRouter();
    
    return (
    <div className="w-full max-w-8xl mx-auto bg-gray-500 bg-opacity-80 rounded-lg p-8 shadow-lg">
        <h1>Dashboard</h1>
    </div>

  );
}
